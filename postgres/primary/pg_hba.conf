# TYPE  DATABASE        USER            ADDRESS                 METHOD
# Allow local connections
local   all             all                                     trust

# Allow connections for replication from the Docker subnet (adjust IP range if necessary)
host    replication     postgres        0.0.0.0/0               md5

# Allow all other connections to the database (adjust for your security needs)
host    all             all             0.0.0.0/0               md5
